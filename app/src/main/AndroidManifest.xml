<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pamekasan.apppamekasan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".editProfile"></activity>
        <activity android:name=".myDetailTicket" />
        <activity android:name=".profileAct" />
        <activity android:name=".successBuyTicket" />
        <activity android:name=".ticketCheckout" />
        <activity android:name=".ticketDetail" />
        <activity android:name=".homeScreen" />
        <activity android:name=".successSignup" />
        <activity android:name=".registerProfile" />
        <activity android:name=".Register" />
        <activity android:name=".signInB" />
        <activity
            android:name=".Login"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".getStarted" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>